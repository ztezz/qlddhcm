FROM node:20-alpine

WORKDIR /app

ENV NODE_ENV=production

COPY package*.json ./
RUN npm ci --omit=dev

COPY backend_guide ./backend_guide

WORKDIR /app/backend_guide

EXPOSE 7860

CMD ["node", "server.js"]
